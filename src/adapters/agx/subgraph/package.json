{
  "name": "agx-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "clean": "graph clean",
    "test": "graph test",
    "create-local": "graph create agx-points --node https://sepolia.graph.zklink.io/node",
    "deploy-local": "graph deploy agx-points --ipfs https://sepolia.ipfs.zklink.io --node https://sepolia.graph.zklink.io/node",
    "remove-local": "graph remove --node https://sepolia.graph.zklink.io/node agx-points"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.68.5",
    "@graphprotocol/graph-ts": "0.32.0",
    "prettier": "^3.2.5"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
