{
  "name": "zk_l3_graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create": "graph create interport --node http://127.0.0.1:8020",
    "deploy": "graph deploy interport --node http://127.0.0.1:8020 --ipfs http://127.0.0.1:5001",
    "remove": "graph remove interport --node http://127.0.0.1:8020",
    "clean": "graph clean",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.71.1",
    "@graphprotocol/graph-ts": "0.35.1",
    "prettier": "^3.2.5"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
